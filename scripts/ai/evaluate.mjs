import fs from 'node:fs';import {args,pipeline,readJsonl} from './common.mjs';
const options=args(),predictionsFile=String(options.predictions??'');if(!predictionsFile)throw new Error('Use --predictions <jsonl>');const cases=readJsonl(String(options.benchmark??'benchmarks/ass-golden-eval.v1.jsonl')),predictions=readJsonl(predictionsFile),metrics=pipeline.evaluatePredictions(cases,predictions),report={benchmark:'ass-golden-eval',benchmark_version:1,model:String(options.model??'candidate'),created_at:new Date().toISOString(),metrics};const output=String(options.output??'eval-report.json');fs.writeFileSync(output,JSON.stringify(report,null,2)+'\n',{flag:'wx'});console.log(JSON.stringify({stage:'evaluation_complete',output,...report}));
