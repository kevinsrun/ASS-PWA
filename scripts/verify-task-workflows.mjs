import assert from 'node:assert/strict';import fs from 'node:fs';import ts from 'typescript';
const helper={exports:{}};new Function('module','exports',ts.transpileModule(fs.readFileSync('src/lib/taskEvidence.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(helper,helper.exports);
for(const evidence of ['Please complete this form by Friday.','Send me your resume.','Please respond with availability.','Apply by October 10.','Registration closes Monday.','Submit your application.'])assert.ok(helper.exports.hasTaskInstruction(evidence),evidence);
for(const evidence of ['Consider applying if interested.','A historical example application due date.','Our research describes an application of physics.','Required textbook: Physics']){if(evidence.startsWith('Required textbook'))continue;assert.equal(helper.exports.hasTaskInstruction(evidence),false,evidence);}
const guard={exports:{}};new Function('require','module','exports',ts.transpileModule(fs.readFileSync('src/lib/classificationGuardrails.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(()=>helper.exports,guard,guard.exports);
for(const [label,text] of [['TASK','Send me your resume.'],['REQUIRED_FORM','Please complete this form.'],['TASK','Please respond with availability.']]){const result=guard.exports.validateExtractedItem({label,evidenceText:text,confidence:.96,dueAt:null,documentType:'email',required:false,section:{id:'source',kind:'miscellaneous',text,location:'Email body'}});assert.equal(result.normalizedType,'task');assert.equal(result.autoCreate,true);}
const models={exports:{}};for(const key of ['GEMINI_MODEL','GEMINI_MODEL_FAST','GEMINI_MODEL_REASONING'])delete process.env[key];new Function('module','exports',ts.transpileModule(fs.readFileSync('src/lib/geminiModels.ts','utf8'),{compilerOptions:{module:ts.ModuleKind.CommonJS}}).outputText)(models,models.exports);assert.equal(models.exports.GEMINI_MODELS.fast,'gemini-3.8-flash');assert.equal(models.exports.GEMINI_MODELS.reasoning,'gemini-3.8-flash');
const persistence=fs.readFileSync('src/lib/supabaseData.ts','utf8');assert.match(persistence,/table === "todos"\s*\?\s*\{ error: null \}/);assert.match(persistence,/deleted_at/);const provider=fs.readFileSync('src/providers/AppProvider.tsx','utf8');assert.match(provider,/persistedTodos/);assert.match(provider,/changedTodos/);assert.match(provider,/\/api\/tasks/);
console.log('Task workflow guardrails passed: application/form/response requests, reference negatives, missing-date forms remain Tasks, centralized intended model, protected Tasks persistence and canonical manual mutation route. Persistence checks are source assertions, not live destructive writes.');
