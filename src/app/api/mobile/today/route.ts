import { NextRequest, NextResponse } from "next/server"; import { requireApiUser } from "@/lib/serverAuth"; import { mobileSnapshot } from "@/lib/mobileApi";
export async function GET(request: NextRequest) { try { const user = await requireApiUser(request); const value = await mobileSnapshot(user.id); return NextResponse.json({ date: value.today, events: value.todayEvents, tasks: value.tasks.slice(0, 5), actions: value.assistantActions.slice(0, 5) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Today failed." }, { status: 401 }); } }
