import { NextRequest, NextResponse } from "next/server"; import { requireApiUser } from "@/lib/serverAuth"; import { executeAssistantActions, type AssistantAction } from "@/lib/assistantActionExecutor";
export async function POST(request: NextRequest) { try { const user = await requireApiUser(request); const body = await request.json() as { actions?: AssistantAction[]; confirmed?: boolean; timeZone?: string }; return NextResponse.json({ results: await executeAssistantActions(user.id, body.actions ?? [], { confirmed: body.confirmed, timeZone: body.timeZone }) }); } catch (error) { return NextResponse.json({ error: error instanceof Error ? error.message : "Schedule action failed." }, { status: 500 }); } }
